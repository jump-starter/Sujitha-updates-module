config:
  target: 'http://ec2-34-207-242-26.compute-1.amazonaws.com'
  phases:
    - duration: 60
      arrivalRate: 10
scenarios:
  - flow:
    - loop:
      - get:
          url: "/api/updates/{{ $randomNumber(1,10000000) }}"
      count: 5000